export class Employee {

    // Unique identifier for the employee
    id: number = 0;

    // Employee's first name
    firstName: string = "";

    // Employee's last name
    lastName: string = "";

    // Employee's email address
    email: string = "";

    // Employee's date of birth (assuming it's a string for simplicity)
    dob: string = "";

    // Employee's age (assuming it's a number)
    age: string = '';

    // Employee's primary address
    address1: string = "";

    // Employee's secondary address
    address2: string = "";

    // Employee's gender
    gender: string = "";

    // Employee's mobile number
    mobileNumber: string = "";

}